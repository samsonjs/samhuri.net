#!/bin/bash

set -euo pipefail

env=${1:-beta}

terraform plan -var-file="${env}.tfvars" \
               -var "env=${env}"  \
               -state="${env}.tfstate"   \
               -out="${env}.tfplan"
